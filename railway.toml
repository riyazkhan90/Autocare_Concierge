[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uv run uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080}"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
