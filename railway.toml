[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uv run uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080} --timeout-keep-alive 75"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
